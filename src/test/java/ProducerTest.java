import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/**
 * The way the code is written now, the producer is hard coded to do everything. This would make testing a bit awkward
 * When the TODOs are implemented, then testing would be much clearer
 * For instance, if producers can write to many topics, this functionality would need to be tested
 */
class ProducerTest {

    @Test
    void sendMessage() {
    }

    @Test
    void run() {
    }
}